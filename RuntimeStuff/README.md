# RuntimeStuff

[![NuGet](https://img.shields.io/badge/nuget-v2026.01.26.2-blue)](https://www.nuget.org/packages/RuntimeStuff)
[![.NET Standard 2.0](https://img.shields.io/badge/.NET%20Standard-2.0-green)](https://docs.microsoft.com/en-us/dotnet/standard/net-standard)
[![License](https://img.shields.io/badge/license-MIT-orange)](LICENSE)

Набор утилит и вспомогательных хелперов общего назначения для .NET проектов.

## 📋 Описание

RuntimeStuff — это библиотека полезных утилит, расширений и инструментов для упрощения разработки на .NET. Библиотека включает в себя:

- 🔤 Расширения для работы со строками
- 📁 Утилиты для работы с файлами
- 💾 Инструменты сериализации
- 🗄️ Simple ORM для работы с базами данных
- 🔍 LINQ-утилиты и расширения
- 🛠️ Другие вспомогательные инструменты общего назначения

## 🚀 Установка

### NuGet Package Manager
```bash
Install-Package RuntimeStuff
```

### .NET CLI
```bash
dotnet add package RuntimeStuff
```

### PackageReference
```xml
<PackageReference Include="RuntimeStuff" Version="2026.01.26.2" />
```

## 💻 Требования

- .NET Standard 2.0 или выше
- Совместимость с:
  - .NET Framework 4.6.1+
  - .NET Core 2.0+
  - .NET 5.0+
  - Xamarin
  - Unity

## 📚 Основные компоненты

### 📝 CsvHelper
Работа с CSV-файлами: парсинг, экспорт, экранирование специальных символов. Поддержка преобразования объектов в CSV и обратно.

**Основные методы:**
- `ToCsv<T>()` — экспорт коллекции объектов в CSV
- `FromCsv<T>()` — импорт из CSV в коллекцию объектов

### 📊 DataTableHelper
Утилиты для работы с `DataTable`: создание, заполнение, конвертация из/в коллекции объектов.

**Основные методы:**
- `ToDataTable<T>()` — преобразование коллекции в DataTable
- `ToList<T>()` — преобразование DataTable в типизированную коллекцию
- `AddRow()` / `AddRows()` — добавление строк
- `AddCol()` — добавление столбцов
- `ContainsRow()` — проверка наличия строки

### 📅 DateTimeHelper
Расширенная работа с датами и временем: парсинг, форматирование, вычисления, получение периодов.

**Основные методы:**
- `BeginDay()` / `EndDay()` — начало/конец дня
- `BeginMonth()` / `EndMonth()` — начало/конец месяца
- `BeginYear()` / `EndYear()` — начало/конец года
- `GetDates()` — получение последовательности дат
- `EachDay()` — перечисление дней в периоде
- `ParseDate()` / `ParseTimeSpan()` — гибкий парсинг дат и интервалов
- `GetElapsedTime()` — вычисление прошедшего времени
- `ExactNow()` — текущее время с микросекундной точностью

### 🔗 EventHelper
Динамическое связывание событий и свойств, создание композитных подписок.

**Основные возможности:**
- `BindEventToAction()` — привязка события к Action через рефлексию
- `BindProperties()` — двустороннее связывание свойств объектов
- Автоматическая отписка через IDisposable

### 🔍 ExpressionHelper
Работа с Expression Trees: извлечение информации о членах типа, получение значений свойств через лямбда-выражения.

**Основные методы:**
- `GetPropertyName()` — извлечение имени свойства из лямбды
- `GetPropertyInfo()` — получение PropertyInfo из выражения
- `GetPropertyInfoChain()` — цепочка свойств (например, `x => x.Parent.Name`)
- `GetValue()` — получение значения свойства через скомпилированное выражение
- `GetMemberInfo()` — извлечение MemberInfo с кэшированием

### 🔎 FilterHelper
Парсинг и применение текстовых фильтров к коллекциям. Поддержка операторов сравнения, логических операций, диапазонов.

**Синтаксис фильтров:**
- Операторы: `=`, `!=`, `<`, `>`, `<=`, `>=`
- Логика: `AND`, `OR`, `NOT`
- Диапазоны: `BETWEEN value1 AND value2`
- Списки: `IN (value1, value2, ...)`
- Текстовый поиск: автоматическая проверка всех текстовых полей

**Методы:**
- `Filter<T>()` — применение фильтра к коллекции
- `FilterByText<T>()` — полнотекстовый поиск по объектам
- `Parse()` — парсинг строки фильтра в Expression

### 📦 JsonSerializerHelper
Легковесная JSON-сериализация без внешних зависимостей. Поддержка объектов, коллекций, словарей, примитивов.

**Возможности:**
- Сериализация сложных объектов
- Поддержка вложенных структур
- Экранирование специальных символов
- Работа с коллекциями и словарями
- `Serialize()` — преобразование объекта в JSON-строку

### 🔀 SortHelper
Динамическая сортировка коллекций по имени свойства в строковом формате.

**Основные методы:**
- `Sort<T>()` — сортировка с указанием направления
- `SortAsc<T>()` / `SortDesc<T>()` — сортировка по возрастанию/убыванию
- `ApplySort()` — применение сортировки к IEnumerable

### 🔤 StringHelper
Мощный набор расширений для работы со строками: парсинг, токенизация, форматирование, сжатие.

**Основные возможности:**
- **Парсинг и извлечение:**
  - `GetTokens()` — извлечение токенов между маркерами

- **Манипуляции:**
  - `Cut()` — вырезание фрагмента
  - `Remove()` — удаление всех вхождений
  - `Replace()` — замена с поддержкой маркеров
  - `RemoveLongSpaces()` — нормализация пробелов
  - `Crop()` — обрезка строки до длины

- **Утилиты:**
  - `Coalesce()` — первая непустая строка
  - `IsJson()` / `IsXml()` — проверка формата
  - `Zip()` / `UnZip()` — сжатие/распаковка GZip
  - `RepeatString()` — повторение строки N раз

### ⏱️ TaskHelper<T>
Вспомогательные методы для асинхронной работы с Task: ожидание с отменой, управление завершением задач.

**Основные методы:**
- `Wait()` — ожидание завершения задачи с поддержкой отмены
- `CancelWait()` — отмена ожидания
- `TryComplete()` — попытка завершить TaskCompletionSource
- `ClearAll()` — очистка всех ожидающих задач

## 🎯 Примеры использования

### CSV Export/Import
```csharp
var users = GetUsers();
string csv = users.ToCsv(); // Экспорт в CSV

var imported = CsvHelper.FromCsv<User>(csv); // Импорт из CSV
```

### DateTime Operations
```csharp
var startOfMonth = DateTime.Now.BeginMonth();
var endOfMonth = DateTime.Now.EndMonth();
var allDays = startOfMonth.GetDates(endOfMonth);
```

### Dynamic Filtering
```csharp
var products = GetProducts();
var filtered = products.Filter(p => p.Price, "Price > 100 AND Category = 'Electronics'");
```

### String Manipulation
```csharp
string html = "<div>Content</div>";
string content = html.FirstAfter("<div>").FirstBefore("</div>"); // "Content"

string compressed = "Long text...".Zip();
string decompressed = compressed.UnZip();
```

### Property Binding
```csharp
var binding = EventHelper.BindProperties(
    source, "PropertyName",
    target, "PropertyName"
); // Двустороннее связывание

// Отписка
binding.Dispose();
```

## 🔧 Разработка

Проект использует следующие инструменты контроля качества кода:

- **StyleCop.Analyzers** — статический анализ стиля кода
- **SonarAnalyzer.CSharp** — анализ безопасности и качества кода
- Полная документация XML для всех публичных API

### Сборка проекта

```bash
# Debug сборка
dotnet build

# Release сборка (warnings = errors)
dotnet build -c Release

# Создание NuGet пакета
dotnet pack -c Release
```

## 📖 Документация

Библиотека включает полную XML-документацию для всех публичных API, доступную через IntelliSense в вашей IDE.

## 🤝 Вклад в проект

Мы приветствуем участие сообщества! Пожалуйста, следуйте нашим стандартам кодирования и убедитесь, что все тесты проходят перед отправкой pull request.

## 📄 Лицензия

Этот проект распространяется под лицензией MIT. См. файл [LICENSE](LICENSE) для получения дополнительной информации.

## 👤 Автор

**Rudnev Sergey**

- GitHub: [@Unholius](https://github.com/Unholius)
- Проект: [RuntimeStuff](https://github.com/Unholius/RuntimeStuff)

## 🔗 Ссылки

- [Репозиторий GitHub](https://github.com/Unholius/RuntimeStuff)
- [NuGet Package](https://www.nuget.org/packages/RuntimeStuff)
- [Отчёты об ошибках](https://github.com/Unholius/RuntimeStuff/issues)

---

⭐ Если вам понравился этот проект, поставьте звезду на GitHub!